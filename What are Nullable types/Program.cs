using System;

//Nullable types - Variable types does not hold null values,
//so to hold the null values we have to use nullable types.
namespace What_are_Nullable_types
{
    class Program
    {
        static void Main(string[] args)
        {
            
            //int i = null;

            //  Valid declaration 
            Nullable<int> j = null;

            // Valid declaration
            int? k = null;

        }
    }
}
